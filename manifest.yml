---
declared-services:
  codecamp-mic-service-standard:
    label: speech_to_text
    plan: standard
applications:
- name: codecamp-mic
  path: .
  command: npm start
  memory: 512M
  services:
    - codecamp-mic-service-standard
  env:
    NODE_ENV: production
    SECURE_EXPRESS: 1
  host: codecamp-mic
